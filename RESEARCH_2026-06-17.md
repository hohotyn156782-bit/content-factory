# Аудит + ресёрч усилений — 2026-06-17 (раунд 3)

## АУДИТ: готовность 72% → ~78% после фиксов этого захода
**Критические баги (мои свежие модули) — ИСПРАВЛЕНЫ:**
- ✅ `topics_db._conn()` — добавлен `check_same_thread=False` + `PRAGMA journal_mode=WAL` + busy_timeout (панель собирает в потоках → была гонка/порча БД). Проверено: WAL активен, 8 параллельных записей ок.
- ✅ `core.append_history()` — обёрнут в `threading.RLock` (параллельные посты не перемешивают строки).

**Внедрено доп. (no-key):**
- ✅ Pollinations Text API как **последний рубеж LLM-каскада без ключа** (GPT-5/Claude через один URL). Каскад не встанет даже при исчерпании всех квот. `llm.py` теперь поддерживает `no_key` провайдеров.

**Осталось из аудита (по приоритету, не критично):**
1. Ротация `history.jsonl`/`performance_log` по 50МБ (рост ~10к записей/год). ~15 строк.
2. Реальный тренд-парсер вместо Groq-заглушки в `scheduler._suggest_topics` (selector.pick_topics УЖЕ реальный — это лишь fallback). 
3. `/api/health` healthcheck + алерт в TG при падении. ~30 строк.
4. Autoclean `CACHE_DIR` (файлы >7 дней). ~8 строк.
- Несколько находок агента были устаревшими (LLM-ключи «пустые» — агент не грузил secrets; ключи на месте).

## РЕСЁРЧ: ТОП к внедрению (всё бесплатно)

### Можно добавить БЕЗ карты, нужен только free-ключ юзера:
- **Cerebras** (cloud.cerebras.ai) — 1М токенов/день, 2000 ток/сек, Qwen3-32B. Слот в каскаде УЖЕ есть → дать `CEREBRAS_API_KEY`. Топ-приоритет.
- **SambaNova** (cloud.sambanova.ai) — Llama 405B/Qwen 72B бесплатно, 200к ток/день. Добавить слот.
- **Trends MCP** (trendsmcp.ai) — Google+YouTube+TikTok+Reddit тренды в одном вызове, 100 запр/мес. Email→ключ. Авто-выбор тем.
- **Gladia** (gladia.io) — 10 ч/мес пословные субтитры, отличный RU. Резерв точных таймингов.
- **Supadata** (supadata.ai) — 100 транскриптов/мес → анализ скриптов конкурентов, обучать генератор на реальных виралах.
- **Tavily** (tavily.com) — 1000 поисков/мес, мониторинг новостных трендов.

### За 0 усилий на существующих ключах:
- **Groq word-timestamps** — `timestamp_granularities:["word"]` в уже работающем Groq Whisper → точные пословные тайминги субтитров (сейчас у EL-голоса они есть из alignment, для edge-фолбэка — улучшит). ~15 мин.

### Для автономной публикации (нужен OAuth/токен):
- **YouTube Data API v3** — 6 загрузок/день бесплатно (адаптер есть, нужен OAuth).
- **VK video.save+wall.post** — инфра есть в lead-hunter, +30 строк (нужен VK user-токен).
- **TikTok Content Posting API** — официальный, 25 видео/день, нужен app-audit (1-7 дней).

### Качество видео (бесплатно/почти):
- **fal.ai** ($20 кредитов, MCP уже подключён) — Real-ESRGAN апскейл кадров/превью, RIFE интерполяция.
- **Piper TTS** (локальный, RU-голоса) — 3-й уровень TTS-каскада (качественнее edge-tts, без сети).

### Отклонено: Replicate (нет постоянного free), Kokoro/AssemblyAI (нет RU), instagrapi (риск бана), Social Blade (платный).
