# Content Factory — путь от парсера до рендера (10 этапов)

Полностью автономная фабрика faceless-видео: тренды → темы → сценарий → озвучка → видео → выкладка.
Всё бесплатно (LLM-каскад, бесплатные источники, сток Pexels), оркестрация — панель + крон.

---

## Схема

```
[1 ПАРСЕР] → [2 СЕЛЕКТОР] → [3 ПЛАН ДНЯ] → [4 СЦЕНАРИЙ] → [5 ОЗВУЧКА]
                                                              ↓
[10 ПУБЛИКАЦИЯ] ← [9 ОЧЕРЕДЬ] ← [8 РЕНДЕР] ← [7 СУБТИТРЫ] ← [6 B-ROLL]
```

---

## Этап 1 — Парсер трендов  `pipeline/parser.py`
Собирает «что сейчас цепляет» по нише из БЕСПЛАТНЫХ источников без ключей:
- **Google News RSS** — новости по ключевикам ниши (главный нишевой сигнал)
- **Reddit .json** — топ постов сабреддитов (идеи/формулировки, особенно EN)
- **Google Trends RSS** — общие дневные тренды страны (geo RU/US)
- **VK newsfeed.search** — посты РФ (переиспользуем VK-токен)

Выход: список кандидатов `{title, source, url, weight}`. Дедуп по заголовку, вес по источнику+вовлечённости.
Конфиг ключевиков/сабреддитов — по категориям ниш (`NICHE_TRENDS` внутри файла).

## Этап 2 — Селектор тем  `pipeline/selector.py`
Кандидаты → скоринг → **LLM-каскад** выбирает N самых перспективных и переформулирует каждый в
КОНКРЕТНУЮ цепляющую тему ролика под нишу (не новостной заголовок). Отсекает политику/чернуху/
NSFW/демонетизируемое и недавние темы (антиповтор). Фолбэк: топ-заголовки как темы.
Пример: новость «что нельзя спрашивать ChatGPT» → тема «ChatGPT сливает твои секреты? 5 вещей, которые НИКОГДА не стоит ему говорить».

## Этап 3 — План дня  `panel/scheduler.py:morning()` + таблица `plan`
На каждую активную связку: тема(ы) от селектора → слоты по площадкам с временами:
**видео** (Instagram/TikTok/YouTube) + **текст** (Threads/VK). Одна тема — на все площадки (кросс-канал).
Запускается кроном в 6:00 МСК; вручную — кнопкой «Построить план на день» в панели.

## Этап 4 — Сценарий  `pipeline/script.py`
LLM-каскад → строгий JSON: **хук + 3-6 сегментов + outro + метаданные** (title/description/caption/hashtags).
Вшит виральный плейбук: топ-хук-формулы 2026, структура удержания (открытая петля → микро-петли →
выплата на 80-90%), бан-лист клише, виральные углы по нишам. Второй проход-редактор чистит язык
(убирает англицизмы/транслит) и докручивает хук. Текстовые посты — `pipeline/textpost.py`.

## Этап 5 — Озвучка  `pipeline/voice.py`
Движок по нише: **XTTS «Luis Moray»** (м, в venv) или **edge «Svetlana»** (ж). Скорость через atempo
(без «бурундука»). Пословный тайминг для субтитров (edge — нативно; xtts — пропорционально).
При сбое XTTS — авто-фолбэк на edge (ролик не теряется).

## Этап 6 — B-roll  `pipeline/broll.py`
Сток **Pexels** по англоязычным запросам сегментов (фолбэк — генеративный градиент `{P}`, без ключей).
Дедуп клипов, аудио стока вырезается (`-an`), источники пишутся в `CREDITS.txt`.

## Этап 7 — Субтитры  `pipeline/subtitles.py`
ASS: крупные группы по 2-3 слова (стиль shorts), DejaVu Sans Bold (кириллица), белый+обводка,
акцентный цвет каждой 4-й группы, тайминг из озвучки, fade.

## Этап 8 — Рендер  `pipeline/assemble.py`
ffmpeg 1080×1920: нормализация клипов (cover-crop, 30fps), склейка, вожжение ASS, микс голос+музыка
(loudnorm −14 LUFS), H.264/AAC, `+faststart`, битрейт-кап. Промежуточные файлы чистятся.

## Этап 9 — Очередь и готовность  панель (вкладка «Очередь») + таблица `content`
Готовые видео/текст → статус `ready`, метаданные и подписи под каждую площадку, кросс-ссылки между
площадками связки. Видно в панели, можно скачать MP4, отметить выложенным.

## Этап 10 — Публикация по крону  `panel/scheduler.py:tick()` + `adapters/`
Каждые 15 мин: слоты, чьё время наступило → выкладка. **YouTube/Instagram/VK — авто** (по токенам
связки `secret_ref`+`ext_id`), **TikTok — вручную** (анти-бан, лежит готовым). Отчёт в Telegram.

---

## Сквозной поток (автопилот)
`крон 6:00 → morning(): parser→selector (тема) → план дня → generate_planned() (сценарий→озвучка→broll→субтитры→рендер) → ready`
`крон */15 → tick(): выкладка по временам (auto для YT/IG/VK, TikTok вручную) → отчёт в TG`

Запуск: панель `bash run_panel.sh` (http://127.0.0.1:8765) · крон-строки в шапке `panel/scheduler.py`.

---

## База (LLM-каскад, бесплатно)  `pipeline/llm.py` + `llm_providers.json`
Этапы 2 и 4 идут через мульти-провайдерный каскад с мульти-ключами и фолбэком:
Gemini (3 акк) → Cerebras → Groq → Mistral → OpenRouter (3 акк) → … При 429/исчерпании — следующий ключ/провайдер.

---

## Research-driven решения (глубокий ресёрч, 7 агентов, 2026)

**Ниши — оставляем все 6** (5 RU + 1 EN), это и есть текущий `niches.json`:
ai_lifehacks (бренд), mind_facts (психология), money_facts (деньги, под-угол «защита от мошенников» — топ-вирал),
history_facts (сторителлинг), personal_brand (студия {P}), ai_lifehacks_en (EN, ниша +340% г/г).
НЕ добавлять: мотивацию/цитаты, true-crime, крипту, детское, чистые листиклы — насыщено/демонетизируется.

**Форматы** (по ROI на подписчиков): №1 сток+TTS+субтитры (наш бэкбон, 22-34с) — основной для всех ниш;
№4 листикл/обратный отсчёт (топ-N, #1 в конце) — дешёвый множитель (script-флаг, *next*);
№5 сторителлинг (история/деньги/психология-эксперимент) — углы уже в `ANGLES`. **Loop-инжиниринг в каждом ролике**
(концовка перекликается с хуком → зацикл = +30% продвижения). НЕ делать: цитаты-карточки, Ken-Burns-слайды, AI-слоп.

**Скоринг тем** (`TOPIC_SCORE`, заложен в selector через веса источников + LLM-выбор; формальный score.py — *next*):
`momentum·0.25 + volume·0.20 + freshness·0.15 + niche_fit·0.15 + pred_retention·0.15 + evergreen·0.05 − dedup·0.05`.
Пороги: ≥75 авто, 55-74 — гейт одобрения в TG (авто-аппрув 30 мин), <55 — отброс.

**Расписание постинга** — в `schedule.json` (МСК, по дням недели, читается `db.slot_for`):
YT ~10:00 (2-3ч до пика), TikTok 19:30 (вручную), IG будни 10-15:00, VK 18-19:00 (1/день макс), Threads 10:00.
Разнос площадок 4-8ч, первые 60 мин отвечать на комменты, без водяных знаков.

**Хуки/удержание** (вшито в `script.py` + ГЕЙТ `validate()`): хук ≤14 слов, одна из топ-формул
(Identity Call / Contrarian / Open Loop / Confession / …), стак 2 триггеров, число/имя/враг;
рестарт-петля на ~25%, твист на ~55-65%; главная выплата на 80-90%; CTA — вопрос/«сохрани», не «лайк-подписка».
Гейт: 75-110 слов, ≥4 сегмента, есть outro, нет бан-фраз — иначе перегенерация (до 3 раз).

**Источники трендов** (`parser.py`, бесплатно без ключей): Google News RSS (главный), HackerNews Algolia (AI/бренд),
Reddit top+rising, Google Trends RSS, VK. *Next:* Telegram t.me/s по курированным RU-каналам — лучший RU geo-сигнал.

## Что ещё в очереди (*next*, заложено в архитектуру)
- `score.py` — формальный 7-мерный скоринг + дедуп vs 90 дней + классификатор «жизни» тренда.
- Telegram t.me/s источник в парсере (курированные RU-каналы по нишам).
- Реальный постинг в `scheduler.tick()` через адаптеры (ждёт твоих токенов IG/YouTube; VK/Threads — готовы креды).
- `performance_log` + обратная связь: сбор просмотров/удержания → ежемесячная рекалибровка весов скоринга.
- Формат-флаг листикла в `script.py`; гейт качества рендера в `build.py` (loudnorm/длительность/субтитры).
- TG-гейт одобрения для тем со score 55-74 (полу-авто).
